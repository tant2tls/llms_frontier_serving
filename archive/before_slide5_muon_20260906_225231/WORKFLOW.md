# Handoff for the next session

Updated 6 September 2026. Start here after README.md. The latest revision updates slide 4 with base-model parameters, active GEMM parameters, routed/selected experts, and attention layers, removing the state-management row. Slides 9 and 13 retain the new result tables. The active comparison remains DeepSeek, GLM and Qwen. No new H100 experiments or GPU harness repairs were performed.

## Completed deliverables

- **Slide 4 parameter comparison (6 September 2026):** shows text-only base parameters excluding MTP/vision: DeepSeek approximately 290.9B, GLM approximately 313.3B, Qwen 176.94B. DeepSeek/GLM are derived from rounded historical tensor buckets; script.md records the arithmetic and GLM's 0.01B bucket-rounding discrepancy. Active GEMM estimates remain 14.08B / 17.38B / 7.27B; experts/selected and layer organization retain the current report's values. Removed the state-management row and revised script 4. Preserved the prior deck and sources under `archive/before_slide4_params_20260906_221919/`. The main deck was initially locked, so built and rendered `artifacts/SyFI_ML_Serving_refined_slide4_staged.pptx`, corrected one overflowing takeaway, then copied the verified file to the main filename after the lock cleared; SHA256 hashes match. Only slide 4 changed visibly, all 24 scripts match notes, and all 24 slides rendered without overflow. Inspected the full-size slide 4 preview. The build/check tools now accept optional `--output` / `--deck` arguments for staged files while keeping their original defaults. No new parameter recount or GPU run was performed.
- **Two result slides (6 September 2026):** replaced only slide 9 (throughput at c1/c4/c16/c64, plus c64 median TTFT/TPOT) and slide 13 (MTP n1 throughput changes and c64 draft acceptance) with editable tables and visible insights. Both use DeepSeek → GLM → Qwen order and model colors. Updated scripts 9/13, regenerated slides.md and the evidence ledger, and rebuilt the deck. Preserved the prior deck/source/documents under `archive/before_results_tables_20260906_220845/`. Verified all 33 displayed table metrics against exact JSONs, all 24 embedded scripts, 8 hidden backups, and that only slides 9/13 changed visibly. PowerPoint rendered all 24 slides with no text overflow after a first COM/RPC failure and retry with desktop access; inspected full-size previews of slides 9/13. The deck now contains 7 charts and the two new result tables. No additional experiments were run.
- **speculative.md (6 September 2026 follow-up):** added three-model MTP n1 comparisons, GLM n5 and GLM long-context comparisons, with output throughput/change, median TTFT/TPOT, draft acceptance, exact JSON links, and per-point dates. Preserved the historical DeepSeek pair and original Qwen base/MTP pair with their limitations. Verified all metrics in 20 comparison rows from 36 unique JSONs, completion/output arithmetic, acceptance arithmetic, and date provenance; workspace audit passed with no issues. The document uses raw DeepSeek MTP c64 rounding of 380.1 tok/s and notes the existing report's 380.2 display discrepancy. Matched reruns, quality checks, phase timing, and harness repairs remain pending; no GPU experiments or raw-evidence edits were performed.
- **throughput.md (6 September 2026 follow-up):** added an explanatory comparison of median TTFT, median TPOT, whole-run input/output rates, and a per-request generation proxy (`1000 / median_tpot_ms`). Includes 12 main batch points and 12 context points, exact JSON links and per-point saved dates, metric definitions, and a worked Qwen c64 calculation. Explicitly marks prefill-only and aggregate decode-only throughput unavailable; preserves Qwen's separate batch/context arms and DeepSeek's context caveat. Verified all 24 rows / 120 displayed metric values, completion, output arithmetic, and saved dates against source JSONs. Workspace audit passed with 95 selected points and no issues. Phase-resolved measurement and existing harness repairs remain pending; no GPU workload or raw-evidence changes were made.
- **User's latest direction:** focus only on DeepSeek-V4-Flash, GLM-5.3-Flash and Qwen3.8-Flash-Next-FP8. Kimi must be added only if explicitly requested later. This overrides the original broader assignment and is now recorded in task.md. Preserve the two-part architecture/measurement structure and emphasize easy comparison using DeepSeek → GLM → Qwen order and orange/purple/teal identities.
- **report.md:** opens with a three-model measurement snapshot and adds a matrix of identical architecture dimensions. Comparison columns/rows are reordered consistently without changing numeric cells. Removed Kimi coverage, cost extrapolation and its Q&A. Existing mechanisms, analytical framework, proposal and evidence boundaries remain.
- **fix_bug.md:** retains all 13 incidents, adds functional/measurement/causal distinctions, a triage route and proposed discriminating validation fixtures. The saved `metric()` last-sample/default-zero behavior is documented. All harness repairs remain pending.
- **script.md:** all 24 spoken scripts remain embedded verbatim; script 4 explains the parameter/layer comparison and scripts 9 and 13 explain the result tables. Main script: 2,285 whitespace-delimited words, including stage cues; 18-minute timing targets plus 2 minutes Q&A. Slide 4's additional non-spoken accounting reference is recorded after its script block.
- **slides.md / SyFI_ML_Serving_refined.pptx:** 16 visible main slides and 8 hidden backups, with 7 native editable charts. Three-column architecture and precision matrices; a main-arm throughput/latency table, TPOT and prefix plots; an MTP result table with each pair's control status; identical-scale c1/c64 cost panels. Part I is slides 2–7; Part II is slides 8–16. No Kimi content remains in visible slides or embedded notes.
- **Build ownership:** `tools/build_powerpoint.py` is the entry point; `tools/presentation_design.py` owns visible content and reads exact arm JSONs. Edit spoken text in script.md, then rebuild; the builder regenerates slides.md and embeds scripts/references in notes.
- **Provenance:** `artifacts/presentation_evidence.json` records chart input paths and saved point dates, including the historical DeepSeek `batch_c*.json` MTP pair. This revision's prior task/documents/design/deck are preserved under `archive/before_three_model_focus_20260906_140304/`; the earlier redesign archive remains. Raw experiment JSONs, logs and launch scripts were not edited.
- **Research sources:** primary optimizer, architecture and speculation sources were checked on 6 September 2026 and cited in report.md and relevant notes. “Zero-day” is explicitly defined as native drafting/day-0 runtime support; no specific separately named zero-day algorithm was supplied.

## Evidence boundaries to remember

1. Qwen corrected batch = base-util082. Its base context/prefix and MTP comparisons retain startup/pool confounds.
2. DeepSeek main = mtp-off-image. The finer grid uses util085-dev20073; do not splice them. The context-16K discrepancy needs a matched rerun.
3. GLM provides the strongest MTP controls. Alternate FP8 KV works, but uses an experimental version-check bypass; numerical parity is unverified.
4. Low modeled bandwidth does not prove a latency/communication bottleneck. Recurrent state can be checkpointed for reuse.
5. Prefix sweeps compare sharing patterns, not cache-on/off. No blanket “all points cold” claim is valid.
6. bench.sh has remaining validation gaps: missing cache scrapes can become zero, positive hit warnings need not reject a point, only zero completions are rejected, and prefix validation is separate.

## Next work, if requested

| User intent | Smallest useful next step |
|---|---|
| Practice presentation | Rehearse script.md against the deck; use fix_bug.md §7 and report.md §9 for questions |
| Refine slides | Edit tools/presentation_design.py and the corresponding script.md text/timing; rebuild/render |
| Repair benchmark reliability | Implement the pending gates in fix_bug.md §6 with meaningful fixtures before any GPU rerun |
| Obtain stronger results | Match startup/pools, repeat/randomize runs, investigate DeepSeek context discrepancy |
| Explain the bottleneck | Capture per-operator prefill/decode traces and hardware counters; then test TP/EP/chunk-size changes |

Do not resume GPU jobs from archived to-do lists. Remote node names, paths, package versions, quotas, and process IDs describe historical sessions. Verify current environment before any authorized rerun.

## Local checks

Completed: `python tools/build_powerpoint.py`; `python tools/audit_workspace.py` (95 selected points across 14 selected arms; 25 registered arms; 24 notes/scripts; 18-minute main timing; no issues); and `python tools/check_powerpoint.py` in installed Windows PowerPoint (24 rendered slides, backups 17–24 hidden, no text overflow).

Latest revision checks verified that all 24 actual PPTX notes contain the corresponding script verbatim, only slide 4 changed visibly from the preceding deck, and the requested parameter, expert and layer values are present. Inspected slide 4 at full size. The workspace audit passed with 95 selected points, 327 local links, and no issues; PowerPoint reported no overflow on all 24 slides. The preceding revision verified all 33 throughput/MTP table metrics, which remain unchanged.

Inspected the contact sheet and full-size architecture, Muon, speculation and research-proposal previews. Rendered text overflow found during the first pass was corrected and the checker rerun successfully. The checker also now handles Unicode diagnostic output without a Windows console encoding failure and saves `artifacts/powerpoint_preview/render_check.json`.

Rerun the workspace audit after current-document edits. Rebuild and render after deck/script changes. The audit proves saved arithmetic/completeness, links, inventory and synchronization only; it does not prove telemetry validity, causality or statistical significance. No training A/B, quality evaluation, dense control, new trace or GPU rerun was performed. The proposed adaptive policy has not been implemented or tested.
